package com.example.demo.services;


import com.example.demo.entities.AddressEntity;

/*This is the service interface where all the methods related to Address table are defined*/
public interface AddressService {

    void addAddress(AddressEntity address);
}
